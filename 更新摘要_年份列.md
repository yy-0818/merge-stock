# 库存表年份列功能更新摘要

**更新日期**: 2026-09-02  
**版本**: v1.2 → v1.3  
**更新人**: Kiro AI Agent

---

## 📋 更新内容

### ✅ 已完成

1. **核心代码修改** (`merge_stock_files.py`)
   - 在 `ColumnMap` 数据类中新增 `year_idx: int | None` 字段
   - 更新 `_parse_column_map()` 函数支持识别"年份"列
   - 更新 `_is_data_column_name()` 函数将"年份"标记为数据列
   - 更新 `_is_numeric_header()` 函数将"年份"标记为数字列
   - 更新 `_remap_column_map_with_drop()` 函数支持 `year_idx` 的重映射
   - 更新 `_compute_used_data_cols()` 函数排除年份列（业务维度字段）

2. **GUI 界面更新** (`gui.py`)
   - 更新列数说明标签："列数: 自动(按拆分数据 + 剔除全空列) · 支持年份列"

3. **文档完善**
   - 创建 `年份列功能说明.md` - 详细使用说明
   - 创建 `CHANGELOG.md` - 版本更新日志
   - 创建 `示例_年份列.py` - 使用示例脚本

---

## 🎯 功能特性

### 1. 灵活位置识别
- **表头名称**: `年份`
- **自动识别**: 无论年份列在表格中的任何位置，都能自动识别
- **兼容性**: 不同子表的年份列位置可以不同

### 2. 样式应用
- **对齐方式**: 右对齐
- **底色**: 循环数据列底色（蓝→绿→黄→橙）
- **数字格式**: 千分位格式 `#,##0;-#,##0;""`
- **字体**: Calibri 11pt

### 3. 排序逻辑
- **不参与排序**: 年份列仅作为数据展示，不影响原有的"类型→型号"排序
- **保持兼容**: 完全向后兼容旧表（无年份列）

---

## 📦 文件清单

### 修改的文件
```
merge_stock_files.py    +35 行修改
gui.py                  +1 行修改
```

### 新增文件
```
年份列功能说明.md       94 行
CHANGELOG.md            62 行
示例_年份列.py          108 行
更新摘要_年份列.md      本文件
```

---

## 🧪 测试情况

### 测试覆盖
✅ 列映射解析测试（3种不同列顺序）  
✅ 数据合并测试（2个子表）  
✅ 样式应用测试（对齐、底色、数字格式）  
✅ 向后兼容测试（无年份列的旧表）

### 测试结果
- 年份列位置识别：**通过**
- 数据列样式应用：**通过**
- 数字格式：**通过**（千分位格式）
- 对齐方式：**通过**（右对齐）
- 底色：**通过**（循环数据列底色）

---

## 🚀 使用方法

### 1. 准备源表
在 Excel 子表中添加"年份"列（任意位置）：

| 客户名 | 年份 | 型号 | 类型 | 1级 | 2级 | D1 | D2 | 辅助列 |
|--------|------|------|------|-----|-----|----|----|----|
| 客户A  | 2024 | M001 | 类型X| 100 | 50  | 10 | 20 | 显示 |

### 2. 运行合并
```bash
# GUI 模式
python gui.py

# CLI 模式
python merge_stock_files.py --src ./源目录 --index ./分类.xlsx --out ./输出目录
```

### 3. 查看结果
合并后的文件会自动应用年份列样式：
- 右对齐，便于纵向比较
- 带底色，便于区分不同列
- 千分位格式（可选，取决于数据）

---

## 📝 注意事项

1. **表头命名**: 必须使用"年份"作为表头名（不区分大小写，自动去空格）
2. **数据类型**: 年份列数值会被格式化为数字（整数）
3. **兼容性**: 旧表（无年份列）仍可正常处理，无需修改
4. **列顺序**: 年份列可放在任意位置，脚本会自动识别

---

## 🔄 向后兼容

✅ **完全兼容旧版数据表**
- 无年份列的表格仍可正常处理
- 不影响原有的合并逻辑
- 不影响原有的样式应用

---

## 💡 后续建议

1. **数据验证**: 可考虑添加年份范围验证（如 1900-2100）
2. **分组功能**: 未来可考虑按年份分组输出
3. **统计功能**: 可添加按年份汇总的统计功能
4. **筛选功能**: 可添加按年份筛选的功能

---

## 📞 技术支持

如有问题或建议，请查看：
- `年份列功能说明.md` - 详细使用说明
- `CHANGELOG.md` - 完整更新日志
- `示例_年份列.py` - 代码示例

---

**更新完成！现在可以正常使用年份列功能了。** ✨
