# Stock Merge 更新日志

## v1.3 - 2026-09-02

### 新增功能
- **年份列支持**：库存表新增"年份"列，自动识别并应用数据列样式
  - 支持灵活列位置（通过表头名自动识别）
  - 应用数据列样式（右对齐、循环底色、数字格式）
  - 不参与排序（保持原有"类型→型号"排序规则）

### 技术变更
- `ColumnMap` 新增 `year_idx` 字段
- `_parse_column_map()` 支持识别"年份"表头
- `_is_data_column_name()` 和 `_is_numeric_header()` 包含年份列
- `_remap_column_map_with_drop()` 处理年份列索引重映射
- `_compute_used_data_cols()` 正确排除年份列（作为维度字段）

### 测试
- 新增 `test_year_column.py` 测试脚本
- 验证不同列位置的年份列识别
- 验证数据列样式应用
- 验证合并输出正确性

---

## v1.2 - 2024-08-11

### 优化
- 列数自动确定：扫描组内所有子表的真实最大列数
- 空列自动移除：对整组合并结果做"数据行全空列"扫描并压缩
- 始终丢弃用户明确不展示的列：辅助列、辅助2、换算率、备注

### 样式改进
- 专业报表样式（client-facing preset）
- Section 斑马纹（奇浅/偶白交替）
- 数据列底色循环（蓝/绿/黄/橙色系）
- 自适应列宽（按内容真实视觉宽度）

---

## v1.1 - 2024-07-25

### 新增功能
- 排序控制列（辅助2）：支持"不排序"标记行保留原顺序
- 色号列全空行剔除：自动移除没有任何库存的伪空行

### Bug 修复
- 修复型号列空行未正确剔除的问题
- 修复列宽计算不准确的问题

---

## v1.0 - 2024-07-17

### 初始版本
- 按分类表分组合并 Excel 库存表
- 自动创建带时间戳的输出目录
- 辅助列过滤（显示/不显示）
- 数据行排序（类型→型号）
- GUI 和 CLI 双模式支持
- 防误删保护（输出路径安全检查）
